筛出每个因子数对应的数的最小值。
void init() //扩展埃式筛法
{
    for (int i = 1; i <= 1e6; i++)
    {
        for (int j = i; j <= 1e6; j+=i)
        {
            a[j]++;
        }
        if(!b[a[i]]) //保证是最小的
        {
            b[a[i]]=i;//下标保存因子数，值对应的最小值
        }
    }
}

